dgraziotin/apache-php is a fork of dgraziotin/lamp, and it is an
Out-of-the-box Apache + PHP Docker image that *just works* on 
Mac OS X (and other platforms).
On OS X, it enables write support for Apache and mounted volumes for them.